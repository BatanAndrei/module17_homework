import {getPercents} from './taskHW-01';

describe('', () => {

  it('', () => {
    expect(getPercents(30, 200)).toBe(60);
    });

    it('', () => {
      expect(getPercents(30, 200)).toBe(80);
      });
});
